const flag = true;

if (!flag) {  // if flag is false then it will print "Flag is true"
    console.log("Flag is true");
} else { // else it will print "Flag is false"
    console.log("Flag is false");
}

const age = 20; // stores 20 as integer

if (age >= 18) { // checks if age is greater than or equal to 18
    console.log("You are eligible for voting");
} else { // else it will print "You are not eligible for voting"
    console.log("You are not eligible for voting");
}
